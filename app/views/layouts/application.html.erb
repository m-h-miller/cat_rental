<!DOCTYPE html>
<html>
<head>
</head>

<body>
  <% button_text = signed_in? ? "Sign Out" : "Sign In" %>
  <% method = signed_in? ? "DELETE" : "GET" %>
  <% action_url = signed_in? ? session_url : new_session_url %>

  <title>Rental99cats</title>
  <h1><%= current_user.user_name if signed_in? %> </h1>

  <form action= "<%= action_url %>" method = "POST" >
    <input type="hidden" name="_method" value="<%= method %>" >
    <input type="submit" value="<%= button_text %>">

    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  </form>

  <%= yield %>

</body>

</html>
